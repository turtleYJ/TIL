# MSA

## 정의

>“마이크로서비스 아키텍처 스타일은 각각 자체 프로세스에서 실행되고 경량 메커니즘(HTTP 리소스 API)과 통신하는 **소규모 서비스 모음**으로 단일 애플리케이션을 개발하는 접근 방식이다. 이러한 서비스는 비즈니스 기능을 중심으로 구축되며 **완전히 독립적으로 배포**할 수 있다.”

>“하나의 큰 어플리케이션을 여러개의 작은 어플리케이션으로 쪼개어 변경과 조합이 가능하도록 만든 아키텍쳐”

레고와 비교할 수 있다. 레고 브럭 하나하나를 쌓아서 어떤 작업물을 만드는 것을 MSA라고 할 수 있다.

그렇다면 이 MSA의 등장배경은 어떻게되며 기존에는 어떤 방식을 통해 작업물을 만들어 왔을까?

### Monolithic Architecture

: 소프트웨어의 모든 구성요소가 한 프로젝트에 통합되어 있는 형태

- 아직까지는 많은 소프트웨어가 Momolithic 형태로 구현되어 있고, 소규모 프로젝트에는 Monolithic Architecture가 훨씬 합리적이다. 간단하며 유지보수가 용이하기 때문.
- 하지만 일정 규모 이상의 서비스, 혹은 수백명의 개발자가 투입되는 프로젝트에서는 Monolithic Architecture가 뚜렸한 한계를 보인다.

Monolithic Architecture의 한계점

1. 서비스/프로젝트가 커지면 커질수록, 영향도 파악 및 전체 시스템 구조의 파악에 어려움이 있다. (전체적 개괄이 어려움)
2. 빌드 시간 및 테스트시간, 그리고 배포시간이 기하급수적으로 늘어난다.
3. 서비스를 부분적으로 scale-out하기가 힘들어진다.
4. 부분의 장애가 전체서비스의 장애로 이어진다.

즉, MSA는 비즈니스 민첩성(Business agility)와 관련이 크다. 서비스나 프로젝트가 크고, 복잡하고, 장기적으로 운영될수록, MSA의 장점이 나타난다.

![Untitled](./msa1.png)

- 각각의 서비스는 그 크기가 작을 뿐, 서비스 자체는 하나의 모놀리틱 아키텍쳐와 유사한 구조를 가짐.
- 각각의 서비스는 독립적으로 배포가 가능해야함.
- 각각의 서비스는 다른 서비스에 대한 의존성이 최소화 되어야함.
- 각 서비스는 개별 프로세스로 구동 되며, REST와 같은 가벼운 방식으로 통신되어야 함.

## MSA의 장단점

### MSA의 장점

MSA는 서비스가 커지면서 생겼던 Monolithic Architecture의 문제점들을 보완해줄 수 있다.

- 배포(deployment) 관점
    - 서비스별 개별 배포 가능(배포 시 전체 서비스의 중단이 없음)
    - 요구사항을 신속하게 반영하여 빠르게 배포할 수 있음.
- 확장(scaling) 관점
    - 특정 서비스에 대한 확장성이 용이함.
    - 클라우드 사용에 적합한 아키텍쳐.
- 장애(failure) 관점
    - 장애가 전체 서비스로  확장될 가능성이 적음
    - 부분적 장애에 대한 분리가 수월함.

이외에도, 신기술의 적용이 유연하고, 서비스를 polyglot하게 개발/운영할 수 있다는 장점이 있다.

### MSA의 장점

Monolithic Architecture은 단순한 아키텍쳐인데 비해 MSA는 보다 복잡한 아키텍쳐로, 전체 서비스가 커짐에 따라 그 복잡도가 기하급수적으로 늘어날 수 있다.

- 성능 - 서비스 간 호출 시 API를 사용하기 때문에, 통신 비용이나, Latency가 그만큼 늘어나게 된다.
- 테스트 / 트랜잭션 - 서비스가 분리되어 있기 때문에 테스트와 트랜잭션의 복잡도가 증가하고, 많은 자원을 필요로 한다.
- 데이터의 관리 - 데이터가 여러 서비스에 걸쳐 분산되기 때문에 한번에 조회하기 어렵고, 데이터의 정합성 또한 관리하기 어렵다.

>정합성 : 데이터들이 서로 일치하는 상태